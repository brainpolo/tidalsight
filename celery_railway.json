{
    "$schema": "https://railway.com/railway.schema.json",
    "build": {
        "builder": "RAILPACK"
    },
    "deploy": {
        "startCommand": "celery -A tidalsight worker --beat --loglevel=info --concurrency=2",
        "restartPolicyType": "on_failure"
    }
}
